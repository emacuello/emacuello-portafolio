---
import type { Proyect, Devops } from '../utils/interfaces.ts';
interface Props {
	aboutProyect: Proyect;
	devops: Devops;
}
const { aboutProyect, devops } = Astro.props;
---

<h1 class="flex justify-center text-3xl font-bold mb-5 text-indigo-500">
	Mi aporte al proyecto
</h1>
<section class="text-gray-400 body-font">
	<div class="container px-5 py-24 mx-auto flex flex-col">
		<div class="lg:w-4/6 mx-auto">
			<div class="rounded-lg h-64 overflow-hidden">
				<img
					alt={aboutProyect.extraImg?.alt}
					class="object-cover object-center h-full w-full"
					src={aboutProyect.extraImg?.url}
				/>
			</div>
			<div class="flex flex-col sm:flex-row mt-10">
				<div class="sm:w-1/3 text-center sm:pr-8 sm:py-8">
					<div
						class="w-20 h-20 rounded-full inline-flex items-center justify-center bg-gray-800 text-gray-600"
					>
						<img
							src="https://avatars.githubusercontent.com/u/84103575?v=4"
							alt="Emacuello logo"
						/>
					</div>
					<div
						class="flex flex-col items-center text-center justify-center"
					>
						<h2
							class="font-medium title-font mt-4 text-white text-lg"
						>
							Emanuel Cuello
						</h2>
						<div class="w-12 h-1 bg-indigo-500 rounded mt-2 mb-4">
						</div>
						<p class="text-base text-gray-400">
							Desarrollador Full Stack & SRE
						</p>
					</div>
				</div>
				<div
					class="sm:w-2/3 sm:pl-8 sm:py-8 sm:border-l border-gray-800 sm:border-t-0 border-t mt-4 pt-4 sm:mt-0 text-center sm:text-left"
				>
					<p class="leading-relaxed text-lg mb-4">
						{aboutProyect.contentExtra}
					</p>
				</div>
			</div>
		</div>
	</div>
</section>

<!-- Mi Aporte -->
<section class="text-gray-400 body-font">
	<div class="container px-5 mx-auto">
		<div class="rounded-lg h-64 sm:h-auto overflow-hidden pb-10">
			<img
				alt={devops.img.alt}
				class="object-cover sm:object-contain h-full sm:h-auto w-full sm:w-auto"
				src={devops.img.url}
			/>
		</div>

		<div class="flex flex-wrap w-full mb-20">
			<div class="lg:w-1/2 w-full mb-6 lg:mb-0">
				<h1
					class="sm:text-3xl text-2xl font-medium title-font mb-2 text-indigo-500"
				>
					{devops.pipelines.name}
				</h1>
				<div class="h-1 w-20 bg-indigo-500 rounded"></div>
			</div>
			<p class="lg:w-1/2 w-full leading-relaxed">
				{devops.pipelines.resumen}
			</p>
		</div>
		<!-- Pipelines -->
		<!-- * -->
		<div class="flex flex-wrap -m-4">
			{
				devops.pipelines.content.map((pipeline) => (
					<div class="xl:w-1/4 md:w-1/2 p-4">
						<div class="bg-[#131313] p-6 rounded-lg">
							<img
								class="h-40 rounded w-full object-cover object-center mb-6"
								src={pipeline.img.url}
								alt={pipeline.img.alt}
							/>
							<h3 class="tracking-widest text-indigo-500 text-xs font-medium title-font">
								{pipeline.environment}
							</h3>
							<h4 class="tracking-widest text-indigo-500 text-xs font-medium title-font">
								{pipeline.trigger}
							</h4>
							<h2 class="text-lg text-white font-medium title-font mb-4">
								{pipeline.name}
							</h2>
							<p class="leading-relaxed text-base">
								{pipeline.content}
							</p>
						</div>
					</div>
				))
			}
		</div>
	</div>
</section>
<!-- ? Deployment -->
<section class="body-font text-gray-400">
	<div class="container px-5 py-24 mx-auto">
		<div class="flex flex-col text-center w-full mb-20">
			<h2
				class="text-xs text-indigo-500 tracking-widest font-medium title-font mb-1"
			>
				{devops.deployment.title}
			</h2>
			<h1
				class="sm:text-3xl text-2xl font-medium title-font mb-4 text-white"
			>
				{devops.deployment.name}
			</h1>
			<p class="lg:w-2/3 mx-auto leading-relaxed text-base">
				{devops.deployment.resumen}
			</p>
		</div>
		<!-- * -->
		<div class="flex flex-wrap">
			{
				devops.deployment.content.map((deploy) => (
					<div class="xl:w-1/4 lg:w-1/2 md:w-full px-8 py-6 border-l-2 border-gray-200 border-opacity-60">
						<h4 class="text-xs text-indigo-500 tracking-widest font-medium title-font mb-1">
							{deploy.envirment}
						</h4>
						<h2 class="text-lg sm:text-xl text-white font-medium title-font mb-2">
							{deploy.tittle}
						</h2>
						<p class="leading-relaxed text-base mb-4">
							{deploy.content}
						</p>
						{/* Todo: Se podria colocar el link a la actions del pipeline, pero todavia no estoy seguro si realizarlo */}
						{/* <a class="text-indigo-500 inline-flex items-center">
											Learn More
											<svg
												fill="none"
												stroke="currentColor"
												stroke-linecap="round"
												stroke-linejoin="round"
												stroke-width="2"
												class="w-4 h-4 ml-2"
												viewBox="0 0 24 24"
											>
												<path d="M5 12h14M12 5l7 7-7 7" />
											</svg>
										</a> */}
					</div>
				))
			}
			<!-- * -->
		</div>
		<!-- Todo: Se podria colocar algo en este boton -->
		<!-- <button
			class="flex mx-auto mt-16 text-white bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg"
			>Button</button
		> -->
	</div>
</section>
<!-- ? Métricas -->
<section class="text-gray-600 body-font">
	<div class="container px-5 py-24 mx-auto">
		<div class="flex flex-col text-center w-full mb-20">
			<h1
				class="sm:text-3xl text-2xl font-medium title-font mb-4 text-indigo-500"
			>
				{devops.metrics.title}
			</h1>
			<p class="lg:w-2/3 mx-auto leading-relaxed text-gray-400">
				{devops.metrics.resumen}
			</p>
		</div>
		<div class="flex flex-wrap -m-4">
			<!-- * -->
			{
				devops.metrics.content.map((metric) => (
					<div class="lg:w-1/3 sm:w-1/2 p-4">
						<div class="flex relative">
							<img
								alt={metric.img.alt}
								class="absolute inset-0 w-full h-full object-cover object-center"
								src={metric.img.url}
							/>
							<div class="px-8 py-10 relative z-10 w-full border-4 border-gray-200 bg-zinc-950 opacity-0 hover:opacity-100">
								<h2 class="tracking-widest text-sm title-font font-medium text-indigo-500 mb-1">
									{metric.title}
								</h2>
								<h1 class="title-font text-lg font-medium text-white mb-3">
									{metric.name}
								</h1>
								<p class="leading-relaxed text-gray-400">
									{metric.content}
								</p>
							</div>
						</div>
					</div>
				))
			}
			<!-- * -->
		</div>
	</div>
</section>
